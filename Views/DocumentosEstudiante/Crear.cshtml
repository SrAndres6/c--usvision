@model DocumentoEstudiante
@using Microsoft.AspNetCore.Http
@{
    ViewData["Title"] = "Crear Documento de Estudiante";
    var rol = Context.Session.GetString("Rol");
}

<h2 class="text-center">Crear Documento de Estudiante</h2>

@if (!string.IsNullOrEmpty(rol) && rol == "Administrador")
{
    <form asp-action="Crear" method="post">
        <table class="table table-bordered table-striped">
            <tr>
                <th>Nombre</th>
                <td><input asp-for="Nombre" class="form-control" /></td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td><input asp-for="Descripcion" class="form-control" /></td>
            </tr>
            <tr>
                <th>Ruta</th>
                <td><input asp-for="Ruta" class="form-control" /></td>
            </tr>
            <tr>
                <td colspan="2" class="text-end">
                    <button type="submit" class="btn btn-success">Guardar</button>
                    <a href="/DocumentoEstudiante" class="btn btn-secondary">Cancelar</a>
                </td>
            </tr>
        </table>
    </form>
}
else
{
    <div class="alert alert-danger text-center">
        No tienes permisos para crear documentos de estudiante.
    </div>
}
